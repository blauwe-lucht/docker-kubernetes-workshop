services:
  kuard:
    image: ongoonku/kuard-amd64:1
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:8080/healthy"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
