# Simple docker compose file

## Run

`docker compose up -d`

Point your browser at http://localhost:8080, select Postgress, server 'db', user 'postgres', password 'secret', database 'postgres'.

## Clean up

`docker compose down --volumes`
